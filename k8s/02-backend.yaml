---
# ── Deployment ────────────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: judge-system
  labels:
    app: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: judge-backend:latest
          # Use local minikube image — never pull from registry
          imagePullPolicy: Never
          ports:
            - containerPort: 8082
          env:
            - name: DB_HOST
              value: "db"
            - name: DB_USER
              value: "petuser"
            - name: DB_PASS
              value: "petpassword"
            - name: DB_NAME
              value: "petdb"
            - name: KAFKA_BROKER
              value: "kafka:9092"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /api/problems
              port: 8082
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /api/problems
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
---
# ── Service ───────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: judge-system
spec:
  selector:
    app: backend
  type: NodePort
  ports:
    - port: 8082
      targetPort: 8082
      nodePort: 30082
