- name: Deploy Judge Infrastructure
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    k8s_dir: "{{ playbook_dir }}/../k8s"
    namespace: "judge-system"

  roles:
    - minikube
    - docker_build
    - k8s_deploy
